# Core packages
streamlit>=1.22

# Pre-built binary packages (faster installation)
numpy==1.24.0
pandas>=2.0
pyarrow==14.0.1
openpyxl>=3.0.0

# Visualization
plotly==5.5.0
matplotlib==3.5.1
altair<5
vega-datasets>=0.9
kaleido>=0.2.1

# PDF handling (slimmed down)
fpdf==1.7.2
pdfplumber==0.7.0
reportlab>=3.6.0
Pillow>=9.0.0
# Previously installed separately in the Dockerfile

# Dependencies
python-dateutil==2.8.2
setuptools==65.5.0
wheel==0.38.0
